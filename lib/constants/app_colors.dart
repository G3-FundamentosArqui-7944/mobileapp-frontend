import 'package:flutter/material.dart';

/// Paleta de colores BodyMatch AI: tono fitness/deportivo, energético y profesional.
class AppColors {
  // Marca
  static const Color primary = Color(0xFFFF6B35); // Naranja energético
  static const Color primaryDark = Color(0xFFE65520);
  static const Color secondary = Color(0xFF0A0E27); // Azul casi negro
  static const Color accent = Color(0xFF00C896); // Verde fitness

  // Roles
  static const Color athleteAccent = Color(0xFF00C896);
  static const Color coachAccent = Color(0xFF4A7DFF);

  // Fondos
  static const Color backgroundDefault = Color(0xFFFFFFFF);
  static const Color backgroundMuted = Color(0xFFF5F7FA);
  static const Color backgroundCard = Color(0xFFFFFFFF);
  static const Color backgroundDark = Color(0xFF0A0E27);

  // Texto
  static const Color textPrimary = Color(0xFF1A1D2E);
  static const Color textSecondary = Color(0xFF6B7280);
  static const Color textMuted = Color(0xFF9CA3AF);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // Estados
  static const Color success = Color(0xFF10B981);
  static const Color warning = Color(0xFFF59E0B);
  static const Color error = Color(0xFFEF4444);
  static const Color info = Color(0xFF3B82F6);

  // Bordes / divisores
  static const Color border = Color(0xFFE5E7EB);
  static const Color divider = Color(0xFFF3F4F6);
}
