<?xml version="1.0" encoding="utf-8"?>
<!--
  Permite tráfico HTTP cleartext solo para desarrollo local.
  - 10.0.2.2  → loopback del emulador Android (mapea al localhost de la PC).
  - 127.0.0.1 → localhost del propio emulador.
  - localhost → alias.
  Producción debe usar HTTPS, por eso el dominio real no está aquí.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
</network-security-config>
